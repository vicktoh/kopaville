export type User = {
    username: string;
    phone: string;
    email: string;
    userId: string;
    password: string;
    photoUrl?: string;
    displayName: string;
    refreshToken?: string;
    phoneNumber?: string;
}