import { Flex } from 'native-base';
import React, { FC, useState } from 'react';
import { string } from 'yup';

type UserTimelineProps = {
    userId: string;
};

export const UserTimeline: FC<UserTimelineProps> = ({ userId }) => {
    
    return (
    <Flex>

    </Flex>);
};
